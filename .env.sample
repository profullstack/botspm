# bots.pm Environment Variables

# System Configuration
NODE_ENV=development  # Options: development, production
LOG_LEVEL=info  # Options: error, warn, info, debug

# Database Configuration
DATABASE_ENGINE=sqlite3  # Options: sqlite3, better-sqlite3

# Application Paths
DATA_DIR=data  # Directory for storing application data
LOGS_DIR=logs  # Directory for storing logs

# Security
ENCRYPTION_KEY=your_encryption_key_for_sensitive_data  # Used for encrypting sensitive data in the database
JWT_SECRET=your_jwt_secret_key  # Used for signing JWT tokens

# API Configuration
API_PORT=3000  # Port for the API server
API_URL=http://localhost:3000  # URL for the API server
WEB_PORT=8080  # Port for the web server

# External Tools
FFMPEG_PATH=/usr/bin/ffmpeg  # Path to FFmpeg executable

# GitHub Release Configuration
GITHUB_TOKEN=your_github_personal_access_token  # Required for the release script
REPO_OWNER=yourusername  # GitHub username or organization
REPO_NAME=bots.pm  # Repository name